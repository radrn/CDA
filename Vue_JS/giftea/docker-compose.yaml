services:
  app:
    container_name: giftea
    build:
      dockerfile: Dockerfile
      target: dev-node
    tty: true
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
    ports:
      - "8001:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true
      - CHOKIDAR_INTERVAL=100
    command: sh -c "npm install && npm run dev"